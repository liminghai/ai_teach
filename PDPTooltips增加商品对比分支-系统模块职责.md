**系统模块职责**：
* 客户端负责用户交互和卡片渲染，以及上报埋点，与Lazzie服务端和埋点服务通信
* Lazzie服务端负责用户对话处理（包含业务逻辑处理、频控、AB灰度、通过逻辑代码解析下游返回的内容组装成信息卡），与TPP通信
* 埋点服务负责接收并持久化客户端上报的埋点信息，只与客户端通信
* tpp推荐工程端负责调用AI大模型进行意图识别，结合算法和知识库进行商品或内容推荐，与Lazzie服务端、AI大模型层、算法、知识库服务通信
